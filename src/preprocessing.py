import pandas as pd

def preprocess_data(train_data: pd.DataFrame, test_data: pd.DataFrame) -> tuple[pd.DataFrame, pd.DataFrame]:
    """
    Preprocess the data
    """
    raise NotImplementedError("preprocess_data method not implemented")